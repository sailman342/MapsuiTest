# MapsuiTest
Double handler call on click

This smal project demonstrates that when you enable double tap

mapView.UseDoubleTap = true;

you get two handler calls (e.NumOfTaps = 1 and e.NumOfTaps = 2)

It is not the expected behavior isn't it ?
